# ServiceAccount 配置
# ServiceAccount 為 Pod 提供身份認證機制
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pod-reader-sa
  namespace: default
  labels:
    app: k8s-pod-lister
    component: serviceaccount
automountServiceAccountToken: false  # 我們將使用 Projected Volume 手動掛載